@model IEnumerable<Content>



<div class="container-fluid">
    <div class="row">

        

        @foreach (var item in Model)
        {
            <div class="col-md-2">
                @Html.Raw(item.Export(new HtmlContentExporter()))
            </div>
        }
    </div>
</div>

